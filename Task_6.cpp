#include <iostream>
#include <cmath>
// 6.3
int main()
{
    double B, C, D, x, p, K, n;
    std::cin >> D >> x >> p >> K;
    B = cos(x);
    C = p - n;
    std::cout << pow(B,2)/(K*D) + B*pow(C,3);
    return 0;
}